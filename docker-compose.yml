version: '3.1'

services:
  db:
    image: mysql:8.0.25
    container_name: db
    restart: always
    secrets:
      - mysql_root
    environment:
      MYSQL_ROOT_PASSWORD_FILE: /run/secrets/mysql_root
      MYSQL_DATABASE: donuts
    volumes:
      - D:/mysql-data:/var/lib/mysql
      - ./mysql-init.sql:/docker-entrypoint-initdb.d/mysql-init.sql
    networks:
      - donuts
  phpmyadmin:
    depends_on:
      - db
    image: phpmyadmin/phpmyadmin:latest
    restart: always
    ports:
      - 8088:80
    environment:
      PMA_HOST: db
    networks:
      - donuts

  voyager_donuts:
    container_name: voyager_donuts
    build:
      context: .
      dockerfile: Dockerfile
    image: voyager_donuts
    environment:
      DB_HOST: db
    volumes:
      - D:/DonutsCalibration:/voyager_calibration
      - D:/DonutsLog:/voyager_log
      - D:/Voyager:/voyager_data
      - D:/DonutsReference:/voyager_reference
      - D:/donuts:/donuts
    networks:
      - donuts

secrets:
  mysql_root:
    file: ./secrets/mysql_root

networks:
  donuts:
